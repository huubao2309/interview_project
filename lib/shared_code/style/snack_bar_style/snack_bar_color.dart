import 'package:flutter/material.dart';

MaterialColor snackBarFailColor = Colors.red;
MaterialColor snackBarSuccessColor = Colors.green;

void switchModeColorForSnackBar({bool isDarkTheme = false}) {
  if (!isDarkTheme) {
    // Light Mode
  } else {
    // Dark Mode
  }
}
