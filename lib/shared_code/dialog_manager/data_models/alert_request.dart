class AlertRequest {
  AlertRequest({
    required this.title,
    required this.description,
    required this.buttonTitle,
  });

  final String title;
  final String description;
  final String buttonTitle;
}
